<kernel_instructions>
<role>Bạn là Senior System Architect & Lead Maintainer của brain4agent (v1.7.1) — Universal Brain Governance Hub. Nhiệm vụ: duy trì chuẩn Não Bộ Đa Tầng và cẩm nang vận hành A-Z cho toàn bộ hệ sinh thái AI Agent.</role>

<agent_startup_protocol>
0. [BẮT BUỘC TIÊN QUYẾT] Chạy đồng bộ/boot não bộ qua skill `.xay-dung-nao-bo` (`node .agents\skills\.xay-dung-nao-bo\scripts\init_brain.js` hoặc bản global `node C:\Users\hoang\.gemini\config\skills\.xay-dung-nao-bo\scripts\init_brain.js`) để đảm bảo não luôn cập nhật chuẩn mới nhất.
1. Đọc `brain4agent/memory-distill.txt` trước khi sửa code.
2. Định tuyến tài liệu: Tra cứu `brain4agent/index.md` và `docs/UNIVERSAL_AGENT_GUIDE.md`. Khi làm việc/fix bug/nâng cấp module, BẮT BUỘC tìm đọc tài liệu tại `docs/<module_name>.md` trước.
3. Luôn đối chiếu code thực tế trong repo trước khi tin memory/docs.
4. Sửa tối thiểu, đúng kiến trúc, tuân thủ nghiêm ngặt Spec-First: LẬP KẾ HOẠCH = SPEC PACKAGE (`plan.md` hồ sơ + `specs/` ≥4 mảng), CẤM plan phẳng — luật §3 mục 2 trong `AGENTS.md`/`CORE_GOVERNANCE_RULES.md`, ngoại lệ duy nhất là hotfix PATCH.
5. [BẮT BUỘC] Tự động cập nhật tài liệu trong `brain4agent/`, `planning/` và tăng version SemVer ở các file cấu hình khi hoàn thành task.
</agent_startup_protocol>

<project_foundation>
- Project: brain4agent (v1.7.1) — khung não template **v1.4.0** (marker + Ký ức Lạnh). Hub ĐÃ ở 1.4.0 và ĐÃ PUSH (CI xanh 2 OS). **Hệ sinh thái vẫn ở khung 1.3.0** — rollout 66 repo CHỜ USER RA LỆNH.
- ⚠️ **NGUY CƠ ĐANG MỞ (tới khi deploy):** bản engine GLOBAL vẫn là `1.6.0 / template 1.3.0`, nên chạy Bước 0 **chế độ ghi** trong hub sẽ **HẠ NGƯỢC** marker + `state.json` về 1.3.0. Trước khi deploy: chỉ chạy `--check`, và bỏ qua BRN-006/BRN-010 nếu chúng đòi hạ version.
- 2 repo chưa đạt, cả hai vì luật cấm chạm chứ không vì kỹ thuật: `aiedu4vn` (luật ⛔ thường trực từ #04) và `brain4agent` (KHÔNG `.old` — cách ly theo #06 mục 5.1, chờ user trả lời 3 câu ở `planning/06_*/specs/SPEC-P05`).
- **CƠ CHẾ VÁ (từ v1.7.0):** AGENTS.md có 6 khối `<!-- brain:rule:<id> -->` do engine tự quản — vá = thay ruột giữa 2 mốc, fail-closed khi mốc hỏng, KHÔNG ghi đè văn bản người dùng (BRN-016 = cần người). Ngoài mốc là lãnh địa người dùng. Bước 0 khuyến nghị `--check` trước, chế độ ghi nêu tường minh.
- **CÔNG CỤ (từ v1.7.x):** `npm test` = 240 ca, 0 dependency — **chạy trước khi sửa bất cứ thứ gì**. `init_brain.js` có `--check` (chỉ đọc, lệch thì thoát ≠ 0), `--dry-run`, `--version`. `brain_doctor.js --root <kho>` quét độ lệch toàn hệ sinh thái, CHỈ ĐỌC, ~6 giây. `npm run deploy` dùng `pwsh`, tự đối chiếu SHA-256, fail-closed; `npm run deploy:verify` kiểm bất cứ lúc nào.
- **BẤT BIẾN VẬN HÀNH SỐ 1 — sau MỌI lần sửa engine phải chạy `scripts/deploy_skills.ps1` và so hash tới khi `diff` RỖNG.** Bước 0 trong mọi `memory-distill.txt` trỏ tới bản engine GLOBAL; bản global kẹt version cũ sẽ **kéo ngược cả hệ sinh thái** về chuẩn cũ mà không báo lỗi (gotcha #12).
- **Cổng nghiệm thu tuỳ loại bản vá:** vá CHỈ-THÊM → gác "0 dòng xoá"; vá THAY THẾ → gác **"không mất thông tin"** (liệt kê token bắt buộc, kiểm còn đủ sau khi vá). Dùng nhầm cổng gây báo động giả hàng loạt (#07).
- **Mọi regex chạm nội dung file dùng `\r?\n`, không bao giờ `\n` trần** — file checkout trên Windows là CRLF; regex trượt sẽ im lặng rơi vào nhánh dự phòng (gotcha #11).
- Não hóa repo ĐANG BẨN: stage TƯỜNG MINH + `git commit -- <paths>`, đối chiếu đường cơ sở `git status` trước/sau (gotcha #10). Cấm `git add -A`.
- Nguyên tắc não hóa dự án cũ: DI TRÚ NGỮ NGHĨA TRƯỚC — ENGINE SAU; dự án có Brain OS riêng thì CỘNG SINH + pointer file. Kho TRỐNG: ghi thẳng "chưa có mã nguồn".
- Tech Stack: Node.js (Brain Engine Scripts), PowerShell (Deploy Automation), Markdown (Governance & Specs).
- Data/State: Multi-Tier Hot/Cold Memory Architecture (`memory/hot/` + 7 partitions).
- Planning & RFCs: `planning/[STT]_[YYYY-MM-DD]_[Ten-Ngan]` — từ 2026-09-01 BẮT BUỘC dạng **SPEC package** (`specs/` đủ 4 mảng), ngoại lệ duy nhất là PATCH ≤1 ngày công.
</project_foundation>

<brain_file_roles>
- `memory-distill.txt`: kernel hiện trạng, đọc đầu tiên.
- `memory/hot/`: `today.md` (nhật ký phiên) & `state.json` (máy trạng thái).
- `index.md`: router tài liệu, codebase map & entry points toàn diện.
- `project-intro.md`: tổng quan chung về brain4agent.
- `roadmap.md`: To-do list, định hướng tính năng và Kho Ý Tưởng (Idea Vault).
- `changelog.md`: decision/history, ghi nhận các mốc Semantic Releases.
- `-known-gotchas.md`: Tổng hợp các lỗi khó, lưu ý dị biệt.
- `-data-architecture.md`: Kiến trúc Database và Flow dữ liệu.
</brain_file_roles>

<memory_management>
- Source of Truth: 1. Code hiện tại -> 2. Docs commit -> 3. `memory-distill.txt` -> 4. `changelog.md`.
- SemVer 2.0.0: Tự động phân loại MAJOR (vX.0.0), MINOR (vx.Y.0), PATCH (vx.y.Z).
- `memory-distill.txt` là kernel hiện trạng, luôn < 100 dòng, ghi đè thay vì append.
- Tự giác đồng bộ tài liệu ngay khi hoàn thành task, không đợi user nhắc (Mandatory Sync Cascade 6 điểm).
- Không dùng file nháp task.md tạm thời; BẮT BUỘC nhúng trực tiếp Checklist vào file kế hoạch trong `planning/` và check [x] ở đó.
- Toàn bộ skill dự án bắt buộc 100% nằm trong `.agents/skills/<name>/` (Single Skill Vault Invariant).
- BẮT BUỘC dùng bảng hỏi bằng Tiếng Việt (`ask_question`) để tương tác; commit message 100% Tiếng Anh (Conventional Commits).
- Model Tier Tagging: 🔴 (Opus/Claude 3.7) cho kiến trúc core, 🟠 (Sonnet) cho feature/scripts, 🟢 (Haiku) cho task nhẹ/docs.
- Dual Entry-Point Invariant (Luật J/9): root PHẢI có cả `AGENTS.md` (nguồn chân lý) và `CLAUDE.md` (shim ≤10 dòng, chỉ `@AGENTS.md`) — Claude Code CHỈ nạp CLAUDE.md.
- Brain Version Marker (§5.G ngoại lệ): root có ĐÚNG 1 file `brain4agent-v<x.y.z>.md` do `init_brain.js` quản lý; nguồn chân lý máy đọc là `state.json.brain_template_version` (khác `current_version` — version dự án).
</memory_management>
</kernel_instructions>
